[project]
name = "cleanup-block-storage-idle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.42.9",
]

[dependency-groups]
dev = [
    "bandit>=1.9.2",
    "ruff>=0.14.9",
]
